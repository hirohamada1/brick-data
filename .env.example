# Which environment to use: local|prod
APP_ENV=local

# Local DB (supabase start)
DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

# Remote DB (set in prod)
# DATABASE_URL=postgresql://...

# Optional if using Supabase client
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Clerk (apps/frontend/.env.local)
